# AGENTS.md

このリポジトリで作業するエージェント向けの指示です。

## プロジェクト概要

`daily-note-generator` は、日次ノートを安定した形式で生成するためのツールです。

目標:

- 日付に基づくファイル名を自動生成する。
- 既存ノートを誤って上書きしない。
- テンプレートを使って、毎日の記録をすぐ始められる状態にする。
- 生成後の手作業による修正を最小限にする。

## 現在の前提

- 実装言語は Python 3.12 以上です。
- 依存管理は `pyproject.toml` で行います。
- Excel 出力には `openpyxl` を使います。
- 日付の既定タイムゾーンは `Asia/Tokyo` です。

## 作業方針

- 変更は、依頼された作業に必要な範囲へ絞ってください。
- 大きな抽象化より、読みやすく予測しやすい実装を優先してください。
- ユーザーが作成したノート、テンプレート、設定ファイルは、明示的な依頼なしに書き換えないでください。
- 既存ノートの上書きは、必ず明確な opt-in 動作にしてください。
- ネットワーク依存の処理は、機能上必要な場合だけ導入してください。
- 破壊的な操作が必要な場合は、対象と理由を説明してから進めてください。

## 期待する動作

- 同じ入力からは同じノートが生成されること。
- ファイル名は一般的なファイルシステムで安全であること。
- 日付処理では、対象日、タイムゾーン、出力フォーマットを明確に扱うこと。
- テンプレートは、手で編集しやすいプレーンテキストを基本にすること。
- CLI を作る場合、出力は短く、成功、作成先、スキップ理由、エラー理由が分かること。
- 既定の起動方法は `python -m daily_note` です。
- 既定の出力先は `daily_notes.xlsx`、シート名は `Notes` です。
- Excel の列は `date`, `time`, `weekday`, `note` です。

## 実装時の注意

- 日付処理、ファイル名、テンプレート展開、上書き判定を変更したら、テストを追加または更新してください。
- 生成結果の fixture やスナップショットを使う場合は、意図が読み取れる最小限の内容にしてください。
- 設定値の名前は説明的にし、不要な互換性破壊を避けてください。
- README ができたら、使い方、コマンド、環境変数、重要な既定値を README にも反映してください。

## 検証

作業を終える前に、該当するものを実行してください。

1. formatter または linter があれば実行する。
2. `python -m pytest` を実行する。
3. 生成ロジックを変更した場合は、実際の生成結果を確認する。
4. 実行できなかった検証があれば、理由と一緒に報告する。

## このファイルの更新

以下が決まったタイミングで、このファイルを更新してください。

- 実装言語
- パッケージマネージャ
- 開発サーバーや CLI の起動コマンド
- formatter、linter、test の実行コマンド
- ディレクトリ構成
- テンプレートや出力先の規約
